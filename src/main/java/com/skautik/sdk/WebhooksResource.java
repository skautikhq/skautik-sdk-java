// Code generated by skautikhq/skautik-sdk. DO NOT EDIT.

package com.skautik.sdk;

import com.skautik.sdk.api.WebhooksApi;
import com.skautik.sdk.model.*;

import java.io.File;
import java.math.BigDecimal;
import java.util.Iterator;
import java.util.List;
import java.util.NoSuchElementException;

/** Subscribe to changes rather than polling for them. A webhook costs nothing against your request quota, which makes it much cheaper than re-reading a market on a timer. */
public final class WebhooksResource {

    private final WebhooksApi api;

    WebhooksResource(WebhooksApi api) {
        this.api = api;
    }

    /**
     * List webhooks
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public WebhookList list() throws ApiException {
        return api.listWebhooks();
    }

    /**
     * Create a webhook
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public Envelope create(WebhookInput webhookInput) throws ApiException {
        return api.createWebhook(webhookInput);
    }

    /**
     * Delete a webhook
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public void delete(String webhookId) throws ApiException {
        api.deleteWebhook(webhookId);
    }

    /**
     * Retrieve a webhook
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public WebhookResponse get(String webhookId) throws ApiException {
        return api.getWebhook(webhookId);
    }

    /**
     * Update a webhook
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public WebhookResponse update(String webhookId, WebhookUpdate webhookUpdate) throws ApiException {
        return api.updateWebhook(webhookId, webhookUpdate);
    }

    /**
     * List deliveries
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public DeliveryList listDeliveries(String webhookId, ListDeliveriesOptions options) throws ApiException {
        ListDeliveriesOptions settings = options == null ? ListDeliveriesOptions.builder().build() : options;
        return api.listDeliveries(webhookId, settings.status(), settings.limit());
    }

    /**
     * List deliveries
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public DeliveryList listDeliveries(String webhookId) throws ApiException {
        return listDeliveries(webhookId, ListDeliveriesOptions.builder().build());
    }

    /**
     * Rotate the signing secret
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public Envelope rotateSecret(String webhookId) throws ApiException {
        return api.rotateWebhookSecret(webhookId);
    }

    /**
     * Send a test delivery
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public void test(String webhookId) throws ApiException {
        api.testWebhook(webhookId);
    }

    /**
     * List available events
     *
     * <p>Requires the {@code webhooks:manage} scope.
     */
    public Envelope listEvents() throws ApiException {
        return api.listWebhookEvents();
    }
}
