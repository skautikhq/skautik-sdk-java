// Code generated by skautikhq/skautik-sdk. DO NOT EDIT.

package com.skautik.sdk;

import com.skautik.sdk.api.ImportsApi;
import com.skautik.sdk.model.*;

import java.io.File;
import java.math.BigDecimal;
import java.util.Iterator;
import java.util.List;
import java.util.NoSuchElementException;

/** Move inventory in from whatever system already holds it. A one-off import handles a migration; an import source keeps a CRM and Skautik in step indefinitely. */
public final class ImportsResource {

    private final ImportsApi api;

    ImportsResource(ImportsApi api) {
        this.api = api;
    }

    /**
     * List import sources
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportSourceList listSources() throws ApiException {
        return api.listImportSources();
    }

    /**
     * Create an import source
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportSourceResponse createSource(ImportSourceInput importSourceInput) throws ApiException {
        return api.createImportSource(importSourceInput);
    }

    /**
     * Delete an import source
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public void deleteSource(String sourceId) throws ApiException {
        api.deleteImportSource(sourceId);
    }

    /**
     * Retrieve an import source
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportSourceResponse getSource(String sourceId) throws ApiException {
        return api.getImportSource(sourceId);
    }

    /**
     * Update an import source
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportSourceResponse updateSource(String sourceId, SourceUpdate sourceUpdate) throws ApiException {
        return api.updateImportSource(sourceId, sourceUpdate);
    }

    /**
     * List imports
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportList list() throws ApiException {
        return api.listImports();
    }

    /**
     * Start an import
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportResponse create(File _file, CreateImportOptions options) throws ApiException {
        CreateImportOptions settings = options == null ? CreateImportOptions.builder().build() : options;
        return api.createImport(settings.format(), settings.mode(), settings.sourceId(), settings.dryRun(), settings.filename(), settings.confirmShrink(), settings.idempotencyKey(), _file);
    }

    /**
     * Start an import
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportResponse create(File _file) throws ApiException {
        return create(_file, CreateImportOptions.builder().build());
    }

    /**
     * Retrieve an import
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportResponse get(String importId) throws ApiException {
        return api.getImport(importId);
    }

    /**
     * List import records
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public ImportRecordList listRecords(String importId) throws ApiException {
        return api.listImportRecords(importId);
    }

    /**
     * List accepted formats and columns
     *
     * <p>Requires the {@code imports:write} scope.
     */
    public Envelope formats() throws ApiException {
        return api.importFormats();
    }
}
